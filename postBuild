git clone -b develop https://github.com/UW-Hydro/pysumma.git; cd pysumma; pip install .
cd ..; git clone -b develop https://github.com/NCAR/summa.git
mv /home/jovyan/summaMakefile /home/jovyan/summa
export F_MASTER=/home/jovyan/summa
#make -f ${F_MASTER}/build/summaMakefile

#export NCDF_PATH=/usr
#export LAPK_PATH=/usr
#cd summa/build && sed -i 's/##INCLUDES =/INCLUDES = -I$(NCDF_PATH)\/include/g' Makefile \
#	 && sed -i 's/##LIBRARIES =/LIBRARIES = -L$(NCDF_PATH)\/lib -lnetcdff -L$(LAPK_PATH)\/lib -llapack -lblas -latlas/g' Makefile
#export F_MASTER=/home/jovyan/summa
#export FC=gfortran
#export FC_EXE=/usr/bin/gfortran
#export INCLUDES='-I/usr/include'
#export LIBRARIES='-L/usr/lib -lnetcdff -lblas -llapack'
#make -f ${F_MASTER}/build/Makefile
